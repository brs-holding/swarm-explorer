# Values that must never appear in a built release.
#
# These are upstream's *development* values, published in the zcash-explorer
# repository this fork came from and therefore known to anyone. They were
# compile-time literals in config/config.exs and in the endpoint module, so the
# image carried them however the operator configured it. They are listed here
# so ci/check-release-secrets.py can prove they are gone — this file is a
# denylist, not a store of anything live. No SWARM secret is ever written here,
# and this file is not copied into the image (see the Dockerfile).
#
# One value per line. Blank lines and lines starting with # are ignored.
#
# Add a line whenever a literal is removed from the tree, so the check keeps
# proving it stayed removed.

# config/config.exs, Endpoint secret_key_base
rVfb1mphofjIChVV5RIHW32JxP+aNNOGR4TaOrFSmEPhywkGmPjNHHj6QKCnMxDq

# config/config.exs, Endpoint live_view signing_salt
a4lss9+vZQHOxErTzxjNU4IuhAaslE0Z

# lib/zcash_explorer_web/endpoint.ex, @session_options signing_salt
vsjpf7KS
